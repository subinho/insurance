import './bootstrap';
// resources/js/app.js
import * as bootstrap from 'bootstrap'; // Bootstrap JS
import 'bootstrap/dist/css/bootstrap.min.css'; // Bootstrap CSS

// Initialize Bootstrap components (e.g., tooltips, modals)
window.bootstrap = bootstrap; // Optional: Make Bootstrap available globally